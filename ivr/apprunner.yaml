version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install pipenv
      - pipenv install-r requirements.txt
run:
  runtime-version: 3.11
  command: sh startup.sh
  network:
    port: 8000