version: 1.0
runtime: python3
build:
  commands:
    build:
      # - pip install -r requirements.txt
      - python -m venv .venv
      - source .venv/bin/activate
      - pip install -r requirements.txt
run:
  runtime-version: 3.11.13
  command: sh startup.sh
  network:
    port: 8000